import Head from 'next/head';
import { withTranslation } from 'react-i18next';
import nextI18nextConfig from '../../next-i18next.config.js';

import Layout from '../layouts/LayoutMDX';

export const X = ({ children }) => <Layout>{children}</Layout>;

export const meta = {
  title: 'MDX test page',
  description: 'MDX test page description',
  author: 'Sledilnik',
  keywords: 'Neki',
};

<Head>
  <title>{meta.title}</title>
  <meta name="description" content={meta.description} />
  <meta name="author" content={meta.author} />
  <meta name="keywords" content={meta.keywords} />
</Head>
# MDX as page

[Home](/) | [About](/about)
[EN](/en) | [SL](/sL) | [IT](/it)

export default withTranslation(['common', 'about'], nextI18nextConfig)(X);
