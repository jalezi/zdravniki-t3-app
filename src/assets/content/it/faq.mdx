import { clsx } from 'clsx';
import Link from 'next/link';
import styles from '@/layouts/ComponentsMDX/ComponentsMDX.module.css';
import { getSiteUrl } from '@/lib/utils/common';
import { GIT_REPO_URL } from '@/lib/constants';

export const siteUrl = getSiteUrl();

export const existingPageHref = `${siteUrl}/faq`;
export const missingContentHref = `${GIT_REPO_URL}/blob/main/src/assets/content/it/faq.mdx`;

# FAQ

## Tracciatore di medici

Deve essere tradotto in italiano

Sfortunatamente, questa pagina non è stata ancora tradotta. Vedere il testo originale in lingua {<Link className={clsx(styles.ComponentsMDX,styles.A)} href={existingPageHref} locale="sl">slovena</Link>}.

{/* Replace with proper link */}
Aiutaci a tradurre questa pagina su {<Link className={clsx(styles.ComponentsMDX,styles.A)}  href={missingContentHref} target="_blank" >GitHub</Link>}.
