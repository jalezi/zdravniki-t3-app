import { clsx } from 'clsx';
import Link from 'next/link';
import styles from '@/layouts/ComponentsMDX/ComponentsMDX.module.css';
import { getSiteUrl } from '@/lib/utils/common';
import { GIT_REPO_URL } from '@/lib/constants';

export const siteUrl = getSiteUrl();

export const existingPageHref = `${siteUrl}/faq/`;
export const missingContentHref = `${GIT_REPO_URL}/blob/main/src/assets/content/en/faq.mdx`;

# FAQ

## Doctor Finder for Slovenia

Needs to be translated into English

Unfortunately, this page exist only in {<Link className={clsx(styles.ComponentsMDX,styles.A)} href={existingPageHref} locale="sl">Slovenian</Link>} language.

{/* Replace with proper link */}
Help as translate it to English on {<Link className={clsx(styles.ComponentsMDX,styles.A)}  href={missingContentHref} target="_blank" >GitHub</Link>}.
